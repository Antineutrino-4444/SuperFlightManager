{
  "name": "super-flight-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node src/index.js",
    "client": "cd client && npx vite --host",
    "build": "cd client && npx vite build",
    "start": "npm run server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
